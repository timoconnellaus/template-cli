{
  "name": "template-update-cli",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "bin": {
    "dev": "./dist/index.js"
  },
  "scripts": {
    "build": "bun build index.ts --outdir dist --target node",
    "test": "vitest run",
    "test:watch": "vitest",
    "dev": "bun run index.ts"
  },
  "dependencies": {
    "commander": "^12.1.0",
    "simple-git": "^3.27.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/node": "^20.0.0",
    "tempy": "^3.1.0",
    "vitest": "^2.1.0"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
